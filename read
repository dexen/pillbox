#!/usr/bin/env pil

(one BailAt)

(when (= "-m" (car (argv)))
	(off BailAt)
	(opt) )

(in (opt)
	(let LineNo 0
		(until (or
			(= LineNo BailAt)
			(eof) )
				(inc 'LineNo)
				(prinl (line T)) ) ) )

(bye)
