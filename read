#!/usr/bin/env pil

(setq BailAt 1)

(when (= "-m" (car (argv)))
	(setq BailAt NIL)
	(opt) )

(in (opt)
	(let LineNo 0
		(until (or
			(= LineNo BailAt)
			(eof) )
				(inc 'LineNo)
				(prinl (line T)) ) ) )

(bye)
